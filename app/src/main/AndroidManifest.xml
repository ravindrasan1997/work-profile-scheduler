<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- Silent backend: granted once via
         `adb shell pm grant com.worksched android.permission.MODIFY_QUIET_MODE`.
         protectionLevel is signature|privileged|development|role, so the
         `development` flag makes it pm-grantable to a normal app (same mechanism
         as WRITE_SECURE_SETTINGS). Stays ungranted until the adb command runs;
         the app falls back to the accessibility-gesture path until then. -->
    <uses-permission android:name="android.permission.MODIFY_QUIET_MODE"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".WorkSchedApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorkSched"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.WorkSched">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".alarm.ToggleActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@style/Theme.WorkSched.Transparent" />

        <receiver
            android:name=".alarm.ToggleReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.worksched.action.TOGGLE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".boot.BootReceiver"
            android:exported="true"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".profile.ResumeReconcileReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
                <action android:name="com.worksched.action.RESUME_RETRY" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".profile.ProfileChangeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MANAGED_PROFILE_ADDED" />
                <action android:name="android.intent.action.MANAGED_PROFILE_REMOVED" />
                <action android:name="android.intent.action.MANAGED_PROFILE_AVAILABLE" />
                <action android:name="android.intent.action.MANAGED_PROFILE_UNAVAILABLE" />
            </intent-filter>
        </receiver>

        <service
            android:name=".service.WorkProfileA11yService"
            android:exported="true"
            android:label="@string/a11y_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>
</manifest>
