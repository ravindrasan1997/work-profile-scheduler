<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.WorkSched" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Transparent, no-titlebar theme for ToggleActivity (fallback wake path only). -->
    <style name="Theme.WorkSched.Transparent" parent="android:Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
